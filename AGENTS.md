# AGENTS.md

This file defines how AI agents (GitHub Copilot, Cursor, Claude Code, etc.) should understand and modify this repository.

Agents MUST treat this file as the source of truth for repository conventions.

---

# 1. Project Overview

This project is a **SvelteKit application** using:

- SvelteKit
- Paraglide for internationalization
- TypeScript
- Vite

The project follows standard SvelteKit architecture and prioritizes:

- SSR compatibility
- accessibility
- maintainable component architecture
- clear separation of UI, logic, and routing

---

# 2. Repository Structure

Agents MUST derive the structure from the actual repository rather than assuming it.

Typical layout:

```
.github/
  agents/       # specialized repository AI agents
  prompts/      # slash command definitions (/svelte, /security, /lint, /i18n, /import-hygiene, /repo-audit)
  workflows/    # GitHub Actions CI (lint, security audit, docker, dependabot)
  AGENTS.md     # agent system documentation

src/
  routes/        # SvelteKit routing
  lib/           # reusable modules and utilities
  components/    # shared UI components
  paraglide/     # generated i18n files
  hooks/         # SvelteKit hooks
  stores/        # global stores

static/          # static assets
tests/           # automated tests
```

If the structure changes, agents MUST update this section.

---

# 3. SvelteKit Conventions

Agents MUST follow SvelteKit best practices.

Routing files:

```
+page.svelte
+page.server.ts
+layout.svelte
+layout.server.ts
+server.ts
```

Rules:

- Server logic belongs in `+page.server.ts`
- Avoid mixing server logic inside components
- Prefer `load()` functions for data loading
- Use SvelteKit navigation APIs instead of manual routing

---

# 4. Component Architecture

Reusable UI components belong in:

```
src/lib/components/
```

Rules:

- Components must be reusable
- Avoid page-specific logic inside shared components
- Prefer composition over inheritance
- Accessibility must be considered

Naming:

```
ComponentName.svelte
```

Example:

```
LanguageSwitcher.svelte
Navbar.svelte
Button.svelte
```

---

# 5. Internationalization (Paraglide)

This project uses **Paraglide for i18n**.

Key principles:

- Messages are generated by Paraglide
- Locale switching must preserve the current route
- Do not manually manipulate translation files

Typical structure:

```
src/paraglide/
```

Agents must:

- use official Paraglide utilities
- avoid bypassing the i18n system
- ensure SSR compatibility when switching locales

---

# 6. Styling Guidelines

Agents should follow the existing styling approach used in the repository.

Rules:

- Prefer reusable UI patterns
- Maintain visual consistency
- Avoid introducing new styling frameworks unless requested

---

# 7. AI Agent Modification Rules

When modifying this repository, agents MUST:

1. Inspect the current repository structure before generating code
2. Avoid introducing unnecessary dependencies
3. Follow SvelteKit conventions
4. Preserve SSR compatibility
5. Maintain accessibility
6. Avoid breaking internationalization

Agents SHOULD:

- prefer reusable components
- prefer clear separation of concerns
- keep code readable and idiomatic

---

# 8. Documentation Maintenance

When the repository structure or architecture changes, agents MUST update this file.

Specifically update:

- Repository Structure
- Tooling
- Development conventions

Agents should regenerate sections rather than appending outdated information.

---

# 9. Code Generation Expectations

Generated code should:

- follow SvelteKit idioms
- use TypeScript where applicable
- avoid unnecessary complexity
- remain production-ready

Agents should prioritize **clarity and maintainability over cleverness**.

# 10. Official MCP Instructions

You are able to use the Svelte MCP server, where you have access to comprehensive Svelte 5 and SvelteKit documentation. Here's how to use the available tools effectively:

### Available MCP Tools:

#### 1. list-sections

Use this FIRST to discover all available documentation sections. Returns a structured list with titles, use_cases, and paths.
When asked about Svelte or SvelteKit topics, ALWAYS use this tool at the start of the chat to find relevant sections.

#### 2. get-documentation

Retrieves full documentation content for specific sections. Accepts single or multiple sections.
After calling the list-sections tool, you MUST analyze the returned documentation sections (especially the use_cases field) and then use the get-documentation tool to fetch ALL documentation sections that are relevant for the user's task.

#### 3. svelte-autofixer

Analyzes Svelte code and returns issues and suggestions.
You MUST use this tool whenever writing Svelte code before sending it to the user. Keep calling it until no issues or suggestions are returned.

#### 4. playground-link

Generates a Svelte Playground link with the provided code.
After completing the code, ask the user if they want a playground link. Only call this tool after user confirmation and NEVER if code was written to files in their project.
